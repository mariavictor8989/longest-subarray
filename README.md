# Longest Subarray Project

This project contains two solutions to find the longest balanced subarray (equal number of 0s and 1s):
1. **Recursive Solution**: A clear, step-by-step approach using recursion.
2. **Efficient Solution**: An optimized approach for better performance.

## How to run
Run `efficient_solution.py` or `recursive_solution.py` directly in any Python environment.
